#include "main.h"
/**
* _islower - Short description, single line
* @x: contains value to be compared
* Return: Always 0.
*/
int _islower(int x)
{
if (x > 'a' && x < 'z')
{
return (1);
}
else
{
return (0);
}
}
